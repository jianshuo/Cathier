// WXS helper for array operations in templates
function includes(arr, item) {
  if (!arr || !arr.length) return false
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === item) return true
  }
  return false
}

module.exports = {
  includes: includes
}
